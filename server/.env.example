# MongoDB Atlas Database URI (Backend Only)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/devforge-ai?retryWrites=true&w=majority

# Clerk Authentication Secret Key (Backend Only - NEVER expose to frontend)
CLERK_SECRET_KEY=sk_test_...

# Google Gemini API Key (Backend Only - NEVER expose to frontend)
GEMINI_API_KEY=AIzaSy...

# GitHub API Personal Access Token (Backend Only - NEVER expose to frontend)
GITHUB_TOKEN=ghp_...

# Server Port
PORT=5000
